#pragma once

namespace tetrad {

enum class Endpoint {
    TAIL,    // - (line endpoint)
    ARROW,   // > (arrowhead)
    CIRCLE,  // o (circle)
    NULL_EP  // no endpoint
};

} // namespace tetrad
